<h2>Rating statistics</h2>

<div class="row">
  <div class="col-xs-3 col-sm-3">
    <%= render 'list', objects: @beers, title:"beers" %>
  </div>
  <div class="col-xs-3 col-sm-3">
    <%= render 'list', objects: @breweries, title:"breweries" %>
  </div>
  <div class="col-xs-3 col-sm-3">
    <%= render 'list', objects: @styles, title:"styles" %>
  </div>
</div>

<h4>Most active users</h4>

<table class="table table-hover">
  <thead>
    <tr>
      <th></th>
      <th>favorite beer</th>
      <th>favorite brewery</th>
      <th>preferred style</th>
      <th>ratings</th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= link_to user.username, user %></td>
        <td><%= link_to user.favorite_beer, user.favorite_beer %>
        <td><%= link_to user.favorite_brewery.name, user.favorite_brewery %></td>
        <td><%= link_to user.favorite_style, user.favorite_style %>
        <td><%= user.ratings.count %>
      </tr>
    <% end %>
  </tbody>
</table>

<h4>Recent ratings</h4>

<ul>
  <% @ratings.each do |rating| %>
    <li> <%= rating %> <%= link_to rating.user.username, rating.user %> </li>
  <% end %>
</ul>

Total of <%= @ratings.count %> ratings given
